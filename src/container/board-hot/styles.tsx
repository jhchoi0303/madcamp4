import styled from "styled-components/macro";

import { THEME_COLOR } from "../../constants";

export const BoardHot = styled.ul`
  margin-bottom: 2rem;
  padding: 0;
  border-bottom: 10px solid ${THEME_COLOR.GRAY};
`;
